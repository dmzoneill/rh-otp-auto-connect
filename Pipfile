[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
fastapi = "*"
uvicorn = {extras = ["standard"], version = "*"}
python-gnupg = "*"
pyotp = "*"
requests = "*"
selenium = "*"

[dev-packages]
black = "*"
flake8 = "*"
isort = "*"
mypy = "*"
pytest = "*"
pytest-asyncio = "*"
httpx = "*"

[requires]
python_version = "3.8"

[scripts]
start = "uvicorn main:app --reload --port 8009"
start-prod = "uvicorn main:app --host 0.0.0.0 --port 8009"
lint = "bash -c 'black --check . && flake8 . && isort --check-only . && mypy .'"
format = "bash -c 'black . && isort .'"
test = "pytest"